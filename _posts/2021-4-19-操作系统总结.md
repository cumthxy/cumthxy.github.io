---
layout: post
title: 操作系统总结
categories: 操作系统总结
description: 操作系统总结
keywords: 操作系统总结
---

## 1.阻塞非阻塞与同步异步



1.非阻塞I/O系统调用 read()操作立即返回可以立即拿到的数据，可以是完成的结果，也可以是不完整的结果，或者是空值。

2.而异步I/O系统调用read()的结果必须是完整的。但是这个操作完成的通知可以延迟。

进程通信层面， 阻塞/非阻塞， 同步/异步基本是同义词

IO 系统调用层面（ IO system call ）层面， **非阻塞 IO 系统调用** 和 **异步 IO 系统调用**存在着一定的差别， 它们都不会阻塞进程， 但是返回结果的方式和内容有所差别， 但是都属于非阻塞系统调用（ non-blocing system call ）







### 2.死锁

1.死锁产生条件

互斥：一个资源每次只被一个进程使用。

请求与保持：一个进程因请求资源而阻塞时，对已获得的资源保持不放。

不可剥夺：进程已获得资源，在未使用之前，不能强行剥夺。

循环等待：若干进程之间形成一种头尾相连接的循环等待闭环。

2.死锁预防

破坏互斥条件:

```
就是在系统里取消互斥、若资源一般不被一个进程独占使用，那么死锁是肯定不会发生的，但一般“互斥”条件是无法破坏的。因此，在死锁预防里主要是破坏其他三个必要条件，而不去涉及破坏“互斥”条件  
```

破坏请求和保持条件：

```
方法1：所有的进程在开始运行之前，必须一次性的申请其在整个运行过程各种所需要的全部资源
方法2：该方法是对第一种方法的改进，允许进程只获得运行初期需要的资源，便开始运行，在运行过程中逐步释放掉分配到，已经使用完毕的资源，然后再去请求新的资源。这样的话资源的利用率会得到提高，也会减少进程的饥饿问题
```

破坏不可剥夺条件

```
当一个已经持有了一些资源的进程在提出新的资源请求没有得到满足时，它必须释放已经保持的所有资源，待以后需要使用的时候再重新申请。这就意味着进程已占有的资源会被短暂的释放或者说被抢占了
```



破坏循环等待条件

```
可以通过定义资源类型的线性顺序来预防，
```



3.死锁避免

```
避免是指进程在每次申请资源时判断这些操作是否安全，典型算法是“银行家算法”。但这种算法会增加系统的开销
```





## awk sed grep



## nginx

**Nginx采用了Linux的epoll模型，epoll模型基于事件驱动机制，它可以监控多个事件是否准备完毕，如果OK，那么放入epoll队列中，这个过程是异步的。worker只需要从epoll队列循环处理即可。**

## jwt



## OAuth 





内核态与用户

\4. Io多路复用介绍

\5.   进程、线程、协程区别、通信方式、适用场景

Io密集型（多协程）、cpu密集型用哪个

怎么实现一个协程

操作系统：

14.操作系统实现锁的方法

15.信号量的具体实现

• 五种I/O模型哪一种实际应用最多（I/O复用）？
• select和epoll有什么区别？两种出发模型

1. 进程间通信，哪种方式最快

2. 进程调度

3.  虚地址和实地址
   \- 进程线程
   \5. 多线程

4. 死锁，以及产生必要条件，如何避免

   进程通信，同步

实际应用中如何选择使用进程还是线程，为什么

相比进程，用线程的缺点是什么

.进程线程区别 切换速度哪个快为什么 进程间通信 怎么避免死锁 银行家[算法](http://www.mianshigee.com/interview/search?q=算法)

进程状态/进程通信/套接字/进程调度[算法](https://www.nowcoder.com/jump/super-jump/word?word=算法)

2.2.进程线程的理解和区别

2.3.进程间通信，管道、消息队列、套接字、共享内存那些

2.4.IO多路复用，select/epoll

八股的话就进程线程区别，然后引申到物理地址，虚拟地址，地址空间等等

进程线程什么区别

协程，适用于什么场景

多进程和多线程的区别，使用多线程一定能够提升并发吞吐量吗

多路复用

作者：新诗美酒聊穷年
链接：https://www.nowcoder.com/discuss/624142?type=post&order=time&pos=&page=1&channel=-1&source_id=search_post_nctrack
来源：牛客网



进程线程区别 

  进程通信都有什么方法 

  消息队列内核是如何实现的 

  信号量Linux是如何实现的，如何进行进程的通信 

  共享内存在哪，有什么用，底层是如何实现的 

  共享内存能放什么样的数据结构，是用什么数据结构实现的 

  共享内存里能放vector吗，为什么 

  信号了解吗，进程的信号通信底层是如何实现的 

  多进程[项目]()写过吗（没有） 

  讲讲I/O模型 

  I/O复用 

  写过epoll吗，现在实现一下（？？？） 

  写过IO模型吗 

  多线程模式下如何通信 

  线程之间的栈能否通信，如何通信，数据互通吗？ 

  线程的信号量如何实现 

  用过什么锁 

  除了互斥锁你还能用什么方法来实现线程同步 

  PV操作如何实现的

操作系统

1.进程线程区别

2.多进程架构与多线程架构有什么区别

3.互斥锁与自旋锁

4.多线程实现一个高并发应用注意细节

问了操作系统:进程线程区别，他俩切换开销主要在哪，进程有哪几个状态，怎么变的，在队列中的进程先选哪个，虚拟内存。